#ifndef DISCRMATHS_H
#define DISCRMATHS_H

// Signatures for summations
short int sumX (int* values, register int length);
short int sumY (int* values, register int length);
short int sumXY (int* x_values, int* y_values, register int length);

#endif

